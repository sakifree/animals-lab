<!DOCTYPE html>
<html lang="en">
    <%- include("../partials/head.ejs") %>
<body>
    <%- include("../partials/header.ejs") %>
    <main>
        <div>
            <form action="/animals/<%= animal._id %>?_method=PUT" method="POST">
                <fieldset>
                    <legend>Edit <%= animal.name %></legend>
                    <label>
                        NAME: <input type="text" name="name" value="<%= animal.name %>" placeholder="Enter Name">
                    </label><br>
                    <label>
                        SPECIES: <input type="text" name="species" value="<%= animal.species %>" placeholder="Enter Species">
                    </label><br>
                    <label>
                        LOCATION: <input type="text" name="location" value="<%= animal.location %>" placeholder="Enter Location">
                    </label><br>
                    <label>
                        LIFE EXPECTANCY: <input type="text" name="lifeExpectancy" value="<%= animal.lifeExpectancy %>" placeholder="Enter Life Expectancy">
                    </label><br>
                    <label>
                        EXTINCT: <input type="checkbox" name="isExtinct" <%= animal.isExtinct ? "checked" : "" %>>
                    </label>
                </fieldset>
                <input type="submit" value="Update <%= animal.name %>">
            </form>
        </div>
    </main>
</body>
</html>