<!DOCTYPE html>
<html lang="en">
    <%- include("../partials/head.ejs") %>
<body>
    <main>
        <p><a href="/animals/new"><button>NEW ANIMAL</button></a></p>
        <div class="index">
            <% for (animal of animals) { %>
                <article>
                    <a href="/animals/<%= animal._id %>">
                        <h3>
                            <%= animal.name %>
                        </h3>
                    </a>
                </article>
            <% } %>
        </div>
    </main>
</body>
</html>