<!DOCTYPE html>
<html lang="en">
    <%- include("../partials/head.ejs") %>
<body>
    <main>
        <div>
            <article>
                <h3><%= animal.name %></h3>
                <h3><%= animal.species %></h3>
                <h3><%= animal.location %></h3>
                <h3><%= animal.lifeExpectancy %> years</h3>
                <h3><%= animal.isExtinct ? "Extinct" : "Not Extinct" %></h3>
                <a href="/animals/<%= animal._id %>/edit"><button>EDIT</button></a>
                <form action="/animals/<%= animal._id %>?_method=DELETE" method="POST">
                    <input type="submit" value="DELETE">
                </form>
                <a href="/animals"><button>INDEX</button></a>
            </article>
        </div>
    </main>
</body>
</html>